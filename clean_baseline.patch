*** Begin Patch
*** Update File: train.py
@@
-# Device-specific setup
-if DEVICE_BACKEND == "cuda":
-    from kernels import get_kernel
-    cap = torch.cuda.get_device_capability()
-    # varunneal's FA3 is Hopper only, use kernels-community on non-Hopper GPUs
-    repo = "varunneal/flash-attention-3" if cap == (9, 0) else "kernels-community/flash-attn3"
-    fa3 = get_kernel(repo).flash_attn_interface
-else:
-    # MPS backend - Flash Attention not available, will use PyTorch native attention
-    fa3 = None
-    cap = None
+# ---------------------------------------------------------------------------
+# Flash‑Attention kernels are not available – we will use native PyTorch SDPA.
+# ---------------------------------------------------------------------------
+fa3 = None   # placeholder (unused)
@@
-        y = fa3.flash_attn_func(q, k, v, causal=True, window_size=window_size)
-        y = y.contiguous().view(B, T, -1)
-        y = self.c_proj(y)
-        return y
+        # ---------------------------------------------------------------
+        # Native PyTorch scaled‑dot‑product attention (SDPA) fallback.
+        # q/k/v are (B, T, H, D); SDPA expects (B, H, T, D).
+        # After attention we transpose back and reshape.
+        # ---------------------------------------------------------------
+        q_sdpa = q.transpose(1, 2)   # (B, H, T, D)
+        k_sdpa = k.transpose(1, 2)
+        v_sdpa = v.transpose(1, 2)
+        y = F.scaled_dot_product_attention(q_sdpa, k_sdpa, v_sdpa, is_causal=True)
+        y = y.transpose(1, 2)       # back to (B, T, H, D)
+        y = y.contiguous().view(B, T, -1)
+        y = self.c_proj(y)
+        return y
*** End Patch

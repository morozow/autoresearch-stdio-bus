*** Begin Patch
*** Update File: train.py
@@
-        y = fa3.flash_attn_func(q, k, v, causal=True, window_size=window_size)
-        y = y.contiguous().view(B, T, -1)
-        y = self.c_proj(y)
-        return y
+        # -----------------------------------------------------------------------
+        # Attention: use Flash‑Attention if the kernel is available; otherwise
+        # fall back to PyTorch's native scaled‑dot‑product attention (SDPA).
+        # -----------------------------------------------------------------------
+        if fa3 is not None:
+            y = fa3.flash_attn_func(q, k, v, causal=True, window_size=window_size)
+        else:
+            # Native SDPA expects tensors in (B, H, T, D) layout
+            q_sdpa = q.transpose(1, 2)
+            k_sdpa = k.transpose(1, 2)
+            v_sdpa = v.transpose(1, 2)
+            y = F.scaled_dot_product_attention(q_sdpa, k_sdpa, v_sdpa, is_causal=True)
+            y = y.transpose(1, 2)   # back to (B, T, H, D)
+        y = y.contiguous().view(B, T, -1)
+        y = self.c_proj(y)
+        return y
*** End Patch

{
  "name": "stdio-bus-swarm",
  "version": "0.1.0",
  "description": "Swarm coordination for autoresearch via stdio_bus protocol",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "swarm": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "dev": "ts-node src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "fast-check": "^3.15.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}